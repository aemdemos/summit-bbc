export default async function decorate(block) {
  // The hero block has a single row with two cells:
  // Cell 1: Image (picture)
  // Cell 2: Text content (heading, description, CTA)
  // No restructuring needed — CSS handles the layout.
}
