export default async function decorate(block) {
  // Row 0 = header (title area) — leave as-is
  // Row 1 = Social Impact (big card: image + blue panel)
  // Row 2 = Environmental Sustainability (small card: image + yellow panel)
  // Row 3 = Human Rights (small card: image + yellow panel)
  // No DOM restructuring needed — layout handled via CSS
}
